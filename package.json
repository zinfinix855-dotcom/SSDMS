{
  "name": "ssdms-monorepo",
  "version": "1.0.0",
  "description": "Sehat Sahulat Department Management System Monorepo",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix ssdms-backend && npm install --prefix ssdms-frontend",
    "backend": "npm run dev --prefix ssdms-backend",
    "frontend": "npm run dev --prefix ssdms-frontend",
    "dev": "concurrently --kill-others -n \"backend,frontend\" -c \"bgBlue,bgGreen\" \"npm run backend\" \"npm run frontend\"",
    "build": "npm run build --prefix ssdms-frontend",
    "lint": "npm run lint --prefix ssdms-backend && npm run lint --prefix ssdms-frontend",
    "test": "npm test --prefix ssdms-backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}